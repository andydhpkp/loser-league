import { loadChannelStatuses, enablePushOnDevice, mutateEmpty } from "../modules/reminder-settings.js";
import { CALENDAR_INSTRUCTIONS } from "../modules/calendar-instructions.js";
import { logout } from "../logout.js";
import { registerPwa } from "../modules/pwa-registration.js";

const byId = (id) => document.getElementById(id);
let pushConfiguration;
let currentPushSubscription;
function stateText(state) { return ({ AVAILABLE: "Setup required", OFF: "Off", VERIFICATION_REQUIRED: "Verification required", VERIFICATION_PENDING: "Verification pending. The link expires after 24 hours.", ENABLED: "Enabled", USER_DISABLED: "User-disabled", TEMPORARILY_UNAVAILABLE: "Temporarily unavailable", ENABLED_CURRENT_DEVICE: "Enabled on this device", SETUP_REQUIRED: "Setup required", PERMISSION_DENIED: "Permission denied or blocked" })[state] || "Temporarily unavailable"; }
function setVisible(id, value) { byId(id).hidden = !value; }
function renderPush(value) { pushConfiguration = value; const supported = "serviceWorker" in globalThis.navigator && "PushManager" in globalThis && "Notification" in globalThis; let state = supported ? value.state : "UNSUPPORTED"; if (globalThis.Notification?.permission === "denied") state = "PERMISSION_DENIED"; byId("pushStatus").textContent = state === "UNSUPPORTED" ? "Unsupported on this browser or device" : stateText(state); setVisible("enablePush", supported && value.state === "AVAILABLE" && globalThis.Notification.permission !== "denied"); byId("pushInstructions").textContent = /iPhone|iPad|iPod/.test(globalThis.navigator.userAgent) ? "On iPhone or iPad: open Loser League in Safari, use Share, choose Add to Home Screen, open the installed app, return here, then enable push." : /Android/.test(globalThis.navigator.userAgent) ? "On Android: use Install or Add to Home Screen, open the installed app, return here, then enable push." : "If your desktop browser offers Install, install Loser League before enabling push."; }
async function refreshPushDevice() {
  if (pushConfiguration?.state !== "AVAILABLE" || !("serviceWorker" in globalThis.navigator)) return;
  const registration = await globalThis.navigator.serviceWorker.ready; currentPushSubscription = await registration.pushManager.getSubscription();
  if (!currentPushSubscription) return;
  const response = await fetch("/api/user/reminders/push/status", { method: "POST", headers: { "Content-Type": "application/json", Accept: "application/json" }, body: JSON.stringify({ endpoint: currentPushSubscription.endpoint }) });
  if (!response.ok) return;
  const status = await response.json(); byId("pushStatus").textContent = `${stateText(status.state)}. ${status.deviceCount} device${status.deviceCount === 1 ? "" : "s"} set up.`; setVisible("enablePush", !status.currentDeviceEnabled); setVisible("disablePush", status.currentDeviceEnabled); setVisible("disableAllPush", status.deviceCount > 0);
}
function renderEmail(value) { byId("emailStatus").textContent = stateText(value.state); byId("maskedEmail").textContent = value.maskedDestination ? `Account email: ${value.maskedDestination}` : ""; setVisible("verifyEmail", ["OFF", "VERIFICATION_REQUIRED"].includes(value.state)); setVisible("enableEmail", value.state === "USER_DISABLED"); setVisible("disableEmail", value.state === "ENABLED"); }
function renderCalendar(value) { byId("calendarStatus").textContent = value.state === "AVAILABLE" ? "Available" : "Temporarily unavailable"; if (value.state === "AVAILABLE") { const link = byId("subscribeCalendar"); link.href = value.webcalUrl; link.hidden = false; byId("copyCalendar").hidden = false; byId("copyCalendar").dataset.url = value.subscriptionUrl; } const providers = [CALENDAR_INSTRUCTIONS.apple, CALENDAR_INSTRUCTIONS.google, CALENDAR_INSTRUCTIONS.outlook]; byId("calendarInstructions").textContent = `${providers.map(({ subscribe, remove }) => `${subscribe} ${remove}`).join(" ")} ${CALENDAR_INSTRUCTIONS.limitations}`; }
async function load() { byId("pageStatus").textContent = "Loading reminder options…"; try { const result = await loadChannelStatuses(); if (result.push.value) { renderPush(result.push.value); await refreshPushDevice(); } else byId("pushStatus").textContent = "Temporarily unavailable"; if (result.email.value) renderEmail(result.email.value); else byId("emailStatus").textContent = "Temporarily unavailable"; if (result.calendar.value) renderCalendar(result.calendar.value); else byId("calendarStatus").textContent = "Temporarily unavailable"; byId("pageStatus").textContent = Object.values(result).some((item) => item.error) ? "Some reminder options could not be loaded. Other options remain available." : "Reminder options loaded."; } catch (error) { if (error.message === "SESSION_EXPIRED") location.href = "/index.html?returnTo=%2Freminder-settings.html"; else byId("pageStatus").textContent = "Reminder options are temporarily unavailable. Return Home and try again."; } }
async function pending(button, action) { button.disabled = true; try { await action(); await load(); } catch (error) { if (error.message === "SESSION_EXPIRED") location.href = "/index.html?returnTo=%2Freminder-settings.html"; else byId("pageStatus").textContent = "That change could not be completed. Try again."; } finally { button.disabled = false; button.focus(); } }
byId("enablePush").addEventListener("click", (event) => pending(event.currentTarget, async () => { const result = await enablePushOnDevice({ configuration: pushConfiguration }); byId("pushStatus").textContent = stateText(result.state); }));
byId("disablePush").addEventListener("click", (event) => pending(event.currentTarget, async () => { if (!currentPushSubscription) return; await fetch("/api/user/reminders/push/subscription", { method: "DELETE", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ endpoint: currentPushSubscription.endpoint }) }); await currentPushSubscription.unsubscribe(); currentPushSubscription = null; }));
byId("disableAllPush").addEventListener("click", (event) => pending(event.currentTarget, async () => { await fetch("/api/user/reminders/push/subscriptions", { method: "DELETE", headers: { "Content-Type": "application/json" }, body: "{}" }); if (currentPushSubscription) await currentPushSubscription.unsubscribe(); currentPushSubscription = null; }));
byId("verifyEmail").addEventListener("click", (event) => pending(event.currentTarget, () => mutateEmpty("/api/user/reminders/email/verification-requests")));
byId("enableEmail").addEventListener("click", (event) => pending(event.currentTarget, () => mutateEmpty("/api/user/reminders/email/enable")));
byId("disableEmail").addEventListener("click", (event) => pending(event.currentTarget, () => mutateEmpty("/api/user/reminders/email/disable")));
byId("copyCalendar").addEventListener("click", async (event) => { try { await globalThis.navigator.clipboard.writeText(event.currentTarget.dataset.url); byId("calendarStatus").textContent = "Subscription link provided"; } catch (_error) { byId("calendarStatus").textContent = "Copy was unavailable. Use Subscribe to Pick Deadline Calendar instead."; } });
byId("subscribeCalendar").addEventListener("click", () => { byId("calendarStatus").textContent = "Subscription link provided"; });
byId("logoutBtn").addEventListener("click", logout);
byId("reloadUpdate").addEventListener("click", () => location.reload());
registerPwa({ onUpdate: () => { byId("updateAvailable").hidden = false; } });
load();
